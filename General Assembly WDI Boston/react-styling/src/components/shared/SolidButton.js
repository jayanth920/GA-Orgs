import React from 'react'
import Button from 'react-bootstrap/Button'
// Code Along: Replace with styled components button
const SolidButton = (props) => <Button>{props.children}</Button>

export default SolidButton
