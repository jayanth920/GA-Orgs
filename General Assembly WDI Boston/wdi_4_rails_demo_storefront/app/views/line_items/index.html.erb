<h1>Your Cart</h1>

<div>
	<% @line_items.each do |item| %>
		<div><%= item.product.name %> 
		<%= button_to "X", remove_item_from_cart_path(line_item_id: item), method: :delete %></div>
	<% end %>
</div>

<%= button_to "Checkout", new_order_path %>