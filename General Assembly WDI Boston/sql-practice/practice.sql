-- Create a database "sql-practice" below:


-- Do not touch this line:
GO

-- Create a new table called "flamingo" with the following fields:
    -- `name` (text)
    -- `born_on` (date)
    -- `mated` (boolean)

-- Create at least the following flamingos:
    -- name: 'Fred', born_on: 1999-02-13, mated: true
    -- name: 'Sally', born_on: 2001-03-23, mated: false
    -- name: 'Ari', born_on: 1993-08-19, mated: false

-- Select all flamingos whose `born_on` is before 2000

-- Select all flamingos whose `mated` is false

-- Update the flamingo with the `name` "Ari" to set `mated` to true

-- Delete the flamingos whose `born_on` is after 2000
