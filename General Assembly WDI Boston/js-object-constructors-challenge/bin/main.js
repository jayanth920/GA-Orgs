#! /usr/bin/env node
'use strict'

const app = require('../lib/challenge')
const Tamagotchi = app.Tamagotchi

// To use the node debugger, uncomment the line `debugger` below and run this file like:
//
// node debug <fileName>
//
// Step through the debugger with 'c' until you reach the breakpoint
// Enter an interactive repl with `repl` to investigate variables and run code manually
// Exit with ctrl-c
//
// debugger
