$().ready(function() {

  // validate the comment form when it is submitted
  $("#commentForm").on('submit', function(){ 
    // What is this?
    // What is $(this)

    // validate the name input field and make sure it's at least 2
    // characters.

    // validate the email input field. It should be not be empty and
    // should be a valid email address. 
    // Hint: Look for a regular expression that check for a valid email address

    // validate the URL input field. It MA be empty. But, if it's NOT
    // empty it should be a valid URL.
    // Hint: Look for a regular expression that check for a valid email address

    // validate the comment textarea. It should not be empty and MUST 
    // be at least 5 characters.
  });
})
