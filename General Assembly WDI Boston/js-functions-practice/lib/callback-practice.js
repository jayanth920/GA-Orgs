'use strict'

// waitThenRun should wait 1 second (1000ms) then run a callback function
// It should accept one callback to be run after the 1 second
// *Hint*: Review the setTimeout docs: https://developer.mozilla.org/en-US/docs/Web/API/WindowOrWorkerGlobalScope/setTimeout
const waitThenRun = function (callback) {

}

// runWithNull is a testing function to see how functions run with a `null` argument
// It should accept one callback function, invoke (call) it, pass it `null`, and return its result
const runWithNull = function (callback) {

}

module.exports = {
  waitThenRun,
  runWithNull
}
