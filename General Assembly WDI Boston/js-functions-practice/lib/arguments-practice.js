'use strict'

// addNums should take in two number values
// It should add the numbers together and return the result
const addNums = function (num1, num2) {

}

// nameChange should take in an object and a string value
// It should modify the object's `name` property too match the string value
// and return the modified object
const nameChange = function (obj, newName) {

}

// removeProp should take in an object and a string property name
// It should remove the property from the obejct
// and return the modified object
// Hint: Review the `delete` operator docs: https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/delete
const removeProp = function (obj, propName) {

}

module.exports = {
  addNums,
  nameChange,
  removeProp
}
