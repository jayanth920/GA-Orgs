const reverseMe = ['a', 'b', 'c', 'd', 'e', 'f']

// write some code below here that will produce a NEW array from the one above,
// with the elements in reverse order. Do not modify the original array.

// goal output: ['f', 'e', 'd', 'c', 'b', 'a']

// see what your code is doing like this:
// console.log(reversedArray)

// BONUS: write a function that can produce a reversed copy of any array.
