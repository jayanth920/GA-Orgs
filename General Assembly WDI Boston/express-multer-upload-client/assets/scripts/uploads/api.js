'use strict'

const createEnc = function (data) {
  return $.ajax({
    // ajax options go here
  })
}

const createMulti = function (data) {
  return $.ajax({
    // ajax options go here
  })
}

module.exports = {
  createEnc,
  createMulti
}
