'use strict'

// Question 1

// Create an object which represents a developer.
// It should have two keys. The first key should be "skills"
// whose value is an array of multiple strings. The second
// key should be "name" whose value is the developer's first name.

const developer = null // replace `null` with your answer

// Question 2

// Create a function which accepts 3 numbers. The function should
// add the first two numbers then multiply that sum by the third number
// and return the result.

const addAndMultiply = function (/* your params here */) {
    /* your response here */
}

// Question 3

// Given an array of random numbers, create a new array that contains
// only the first and last elements of the original array.
// For example, if the original array is [23, 4, 12, 9], then firstAndLast
// should be [23, 9]

// DO NOT CHANGE THIS ARRAY
const randomNums = [Math.random(), Math.random(), Math.random(), Math.random()]

const firstAndLast = null // replace `null` with your answer

// Question 4

// Create a function that accepts an array of strings and iterates over each
// string adding an "!" to the end. For example, if the original array is
// ['Hey', 'LOL', 'Woo hoo'], the return value should be
// ['Hey!', 'LOL!', 'Woo hoo!']

const excite = function (/* your params here */) {
    /* your response here */
}

// Question 5

// Create a function that accepts an array of positive numbers and returns
// a new array with the negative version of each number. For example, if the
// original array is [1, 98, 52, 10], the return value should be
// [-1, -98, -52, -10]. NOTE: You must not modify the original array.

const negativeNums = function (/* your params here */) {
    /* your response here */
}

// don't touch anything below here

module.exports = {
    developer,
    addAndMultiply,
    randomNums,
    firstAndLast,
    excite,
    negativeNums
}
