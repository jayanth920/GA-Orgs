// All we need to declare a variable is the keyword 'var'
// and an identifier.

// foo is declared BUT it's not defined.
var foo;
console.log("foo is " + foo);

// bar is declared AND it's defined.
var bar = 3;
console.log("bar is " + bar);

name
//console.log("name is " + name);
