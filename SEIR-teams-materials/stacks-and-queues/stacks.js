class Stack {
	constructor() {
		// should be initialized with an items property set to an empty array
	}

	push(item) {
		// should add item to the top (end) of the stack
	}

	pop() {
		// should remove an item from the top (end) of the stack and return it
		// if stack is empty, return 'Empty stack!'
	}

	peek() {
		// should retrieve the item at the top (end) of the stack
		// if stack is empty, return 'Empty stack!'
	}

	isEmpty() {
		// should return a boolean
		// true if the stack is empty
		// false if the stack is not empty
	}
}

module.exports = { Stack };
