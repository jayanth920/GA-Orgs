function togglePlayer () {
    // change player turn
}


function currentChip () {
    // return which token to use for the turn
}


function incrementChecks () {
    // increase move count, increase player's score in
    // given row, col, and diagonal
}


function checkWin () {
    // See if any player has gotten to 3 in a given row col or diagonal
}


function checkReset () {
    // See if a player has won or the number of moves is over the threshold
}


function boxSelect () {
    // Run all other methods when the box is selected
}