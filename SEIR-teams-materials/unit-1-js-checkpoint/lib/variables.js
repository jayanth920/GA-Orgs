/*
 * Work through each of the prompts below. Update `index.html` with the link to
 * this file to see your results in the console.
 *
 * Pro Tip: Comment out your solution to a prompt before moving on to the next
 * one.
 */

/*
 * Prompt 01:
 *
 * Create a variable called message using var and assign it the value 'Hello
 * World'
 */

var message;

/*
 * Prompt 02:
 *
 *  Create a variable called name using let and assign it the value of your
 *  first name.
 */
let name;
/*
 * Prompt 03:
 *
 * Create a variable called favoriteFood using const and assign it the value of your
 * favorite food.
 */
const favoriteFood = null;
/*
 * Prompt 04:
 *
 * Create a variable called untypedNumber and assign a number to it. On separate lines
 * for each, assign a string, another number, and a boolean value to the
 * variable.
 *
 * What do we need to use to define variables we can change?
 */
let untypedNumber;

////////////////////////////////////////
// DO NOT MODIFY CODE BENEATH THIS LINE
////////////////////////////////////////
if (!this.window) {
	module.exports = {
		message,
		name,
		favoriteFood,
		untypedNumber,
	};
}
