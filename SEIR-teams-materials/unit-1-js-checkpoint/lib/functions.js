/*
 * Work through each of the prompts below. Update `index.html` with the link to
 * this file to see your results in the console.
 *
 * Pro Tip: Comment out your solution to a prompt before moving on to the next
 * one.
 */

/*
 * Prompt 01:
 *
 * Create a function called helloWorld that returns the string "Hello World"
 */

const helloWorld = () => {
	return '';
};

/*
 * Prompt 02:
 *
 * Create a function called printMessage that takes a single parameter and returns it.
 */

const printMessage = () => {
	// your code here
	return;
};

/*
 * Prompt 03:
 *
 * Create a function called greetMe that takes a parameter name and returns the string "Hello
 * Zakk", if 'Zakk' is passed in as the name argument.
 */
function greetMe() {
	// your code here
}
/*
 * Prompt 04:
 *
 * Write a function printCool that accepts one parameter, name as an argument.
 * The function should return the name and a message saying that that person is
 * cool.
 *
 * i.e. printCool("Captain Reynolds") // => "Captain Reynolds is cool."
 */

function printCool() {
	// your code here
}

/*
 * Prompt 05:
 *
 * Write a function called calculateCube that takes a single number and returns the
 * volume of a cube made from that number:
 *
 * i.e. calculateCube(5) // => 125
 * Bonus: can you make this a single-line function?
 */
const calculateCube = () => {
	// your code here
};
/*
 * Prompt 06:
 *
 * Create a function that called getUpperCaseString takes a string as a parameter and returns the
 * uppercase version of the string (using the toUpperCase) method.
 *
 * i.e., getUpperCaseString('hello world') // => 'HELLO WORLD'
 * Bonus: Use a concise-body arrow function
 */
const getUpperCaseString = () => {
	// your code here
};

/*
 * Prompt 07:
 *
 * Write a function called reverseSentence that takes a sentence as a parameter and returns the words
 * in the sentence in reverse order.
 *
 * i.e., reverseSentence("take me home country roads") // => "roads country home me take"
 */

const reverseSentence = () => {
	//  your code here
};

/*
 * Prompt 08:
 *
 * Write a function called formatPhoneNumber that takes a phone number (as a number) and returns
 * a formatted string. So if the number 2025171777 is pass in, the string
 * '(202) 517-1777' will be returned
 */

const formatPhoneNumber = () => {
	//  your code here
};

////////////////////////////////////////
// DO NOT MODIFY CODE BENEATH THIS LINE
////////////////////////////////////////
if (!this.window) {
	module.exports = {
		helloWorld,
		printMessage,
		greetMe,
		printCool,
		calculateCube,
		getUpperCaseString,
		reverseSentence,
		formatPhoneNumber,
	};
}
